<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.sandbox</groupId>
		<artifactId>central</artifactId>
		<version>1.3.3-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<artifactId>sandbox_distribution_verify</artifactId>
	<packaging>jar</packaging>
	<name>Sandbox Distribution Verification</name>
	<description>Cross-platform Java verification of the standalone IDE product and p2 update site.</description>
	<properties>
		<!-- This is a normal Maven/Java module, not an OSGi bundle. -->
		<tycho.mode>maven</tycho.mode>
		<!-- Override on platforms where the Python 3 launcher has another name. -->
		<python.executable>python3</python.executable>
	</properties>
	<build>
		<plugins>
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<configuration>
					<excludeFilterFile>${project.basedir}/spotbugs-exclude.xml</excludeFilterFile>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.6.3</version>
				<executions>
					<execution>
						<id>verify-distribution</id>
						<phase>verify</phase>
						<goals>
							<goal>java</goal>
						</goals>
						<configuration>
							<mainClass>org.sandbox.distribution.DistributionVerifier</mainClass>
							<arguments>
								<argument>${maven.multiModuleProjectDirectory}</argument>
							</arguments>
							<classpathScope>runtime</classpathScope>
							<cleanupDaemonThreads>false</cleanupDaemonThreads>
						</configuration>
					</execution>
					<execution>
						<id>write-publishable-distribution-evidence</id>
						<phase>verify</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>${python.executable}</executable>
							<workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
							<arguments>
								<argument>${maven.multiModuleProjectDirectory}/.github/scripts/verify_distribution_artifacts.py</argument>
								<argument>--repository</argument>
								<argument>${maven.multiModuleProjectDirectory}/sandbox_updatesite/target/repository</argument>
								<argument>--products</argument>
								<argument>${maven.multiModuleProjectDirectory}/sandbox_product/target/products</argument>
								<argument>--output</argument>
								<argument>${maven.multiModuleProjectDirectory}/target/distribution-verification</argument>
							</arguments>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
