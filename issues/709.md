### 1.1 PatternParser erweitern
- [x] Erkennung von `$name$`-Syntax (bereits in `PatternParser` und zugehörigen Tests vorhanden)
- [x] Parsing zu einem speziellen AST-Marker-Node (testspezifische Bindung z.B. `$args$`)
- [x] Unterstützung in Argumentlisten (`method($args$)` wird korrekt geparst)
- [x] Unterstützung in Statement-Sequenzen `{ $stmts$; }` (Tests wie `testBlockPatternMatchesAllStatements` zeigen erfolgreiche Bindung)

### 1.2 PlaceholderAstMatcher erweitern
- [x] Matching-Logik vorhanden (via `PlaceholderAstMatcher`) für:
  - Variadic Placeholders in `arguments` und `statements` (siehe Codeanalysen)
- [x] Greedy/NON-Greedy Defaults (bereit; Variadic Beispiel-Listenbindungen getestet)
- [ ] Bindings als `List<...>` (noch offen)

### 1.3 Tests
- [x] Abdeckung für `$args$`, einschließlich Mixed-Type Coverage.

---

*Updated on 2026-02-14 18:42:31 UTC*