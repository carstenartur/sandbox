# Deliberately narrow profile for the first upstream corpus scenario.
# The JUnit 4 master switch remains off so the evidence is attributable to the
# planned JUnit 3 hierarchy migration.
cleanup.junitcleanup=false
cleanup.junit3cleanup=true
cleanup.junitcleanup_3_test=true
