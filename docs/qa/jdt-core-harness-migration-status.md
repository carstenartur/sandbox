# JDT Core harness migration status

The first implementation slice is executable and fail-closed. It detaches only a single-test leaf whose constructor and suite method are exact discovery boilerplate and whose test body uses no JDT-specific harness state. All stateful, filtered, ordered, inherited, compliance-multiplied and performance-aware shapes remain explicitly unsupported.
