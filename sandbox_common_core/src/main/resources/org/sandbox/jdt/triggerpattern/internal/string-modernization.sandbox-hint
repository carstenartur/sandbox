$str.indexOf($sub) != -1
=> $str.contains($sub)
;;

$str.indexOf($sub) == -1
=> !$str.contains($sub)
;;

$str.indexOf($sub) >= 0
=> $str.contains($sub)
;;

$str.indexOf($sub) < 0
=> !$str.contains($sub)
;;