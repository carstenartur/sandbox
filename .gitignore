# Maven build artifacts
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Eclipse IDE files
.metadata
bin/
tmp/
*.tmp
*.bak
*.swp
*~.nib
local.properties
.loadpath
.recommenders
.externalToolBuilders/
.cproject
.autotools
.factorypath
.buildpath
.target
.tern-project
.texlipse
.springBeans
.recommenders/
.apt_generated/
.apt_generated_test/
.cache-main
.scala_dependencies
.worksheet
# Note: .settings/ and .project files are intentionally tracked for Eclipse plugin projects
# Note: *.launch files are intentionally tracked for Eclipse launch configurations

# Java build artifacts
*.class
hs_err_pid*

# Maven/Tycho build artifacts in target directories
target/

# Logs
*.log

# Archives
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Exclude JAR files except in specific directories for p2 repository
# (Note: Some JAR files in plugins/, features/, and root are part of p2 update site)
# Future binary JAR files should not be added unless they are build artifacts
*.jar
# Exception: Allow JAR files that are already tracked in p2 directories
!plugins/*.jar
!features/*.jar
!artifacts.jar
!content.jar
!sandbox1.2.1/**/*.jar
# Test resources JAR files (stub libraries)
!**/testresources/*.jar

# JaCoCo
*.exec
jacoco*.xml

# OS files
.DS_Store
Thumbs.db
*.backup

# Test reports
test-report.md
test-report.json

# Python cache files
__pycache__/
*.pyc
*.pyo
